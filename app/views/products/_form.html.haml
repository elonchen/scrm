= simple_form_for(@product) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :price
    = f.input :description

  .form-actions
    = f.button :submit, :class=>"btn btn-default"
